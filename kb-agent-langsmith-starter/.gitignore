node_modules/
.env.local
.eve/
.data/
*.tsbuildinfo

.vercel

# Local deployment reference (variable NAMES and sources only, never secrets).
# Documents the Vercel configuration for auditing; must never be committed.
vercel.env
