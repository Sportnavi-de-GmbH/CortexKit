node_modules/
.env
.env.*
!.env.local.example
dist/
.eve/
.output/
__pycache__/
*.pyc

# Runtime observability state: LangSmith trace anchors and the captured system
# prompt (lib/langsmith.ts file stores), span-debug logs, load-test output.
# Never commit — the prompt store can contain conversation-derived text.
.data/

# Next.js dev console build artifacts
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# ── Convex build ─────────────────────────────────────────────────────────────
# Codegen output. Regenerated by `npx convex dev` / `npx convex codegen`;
# never edit by hand.
convex/_generated/

# The Supabase -> Convex data snapshot (scripts/export-from-supabase.ts).
# ~50 MB of real partner data including 2,333 x 1536-dim embeddings. Not a
# source artifact: regenerate it with `npm run convex:export`.
data/export/

# Benchmark output (scripts/benchmark-backends.ts).
benchmarks/results/

# Local anonymous Convex backend state (CONVEX_AGENT_MODE=anonymous).
.convex/
